package com.mieso.app.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Palette
val BrandRedOrange = Color(0xFFD32F2F)  // A deeper, more refined primary red
val BrandOrangeLight = Color(0xFFFF7043) // A vibrant accent for secondary elements
val BrandYellow = Color(0xFFFFA000)   // A warm, amber-like yellow for highlights

val LightBackground = Color(0xFFFBFBFB) // A clean, almost-white background
val LightSurface = Color(0xFFFFFFFF)     // Pure white for cards, sheets
val LightOnPrimary = Color(0xFFFFFFFF)   // Text/icons on primary color
val LightOnBackground = Color(0xFF1B1B1B) // High-contrast dark text
val LightOnSurface = Color(0xFF333333)    // Slightly lighter text for secondary info
val LightOutline = Color(0xFFDCDCDC)     // Subtle outlines and dividers

// Dark Theme Palette
val BrandRedOrangeDark = Color(0xFFE57373) // A softer red for dark mode
val BrandOrangeDark = Color(0xFFFFAB91)  // Lighter accent for dark theme

val DarkBackground = Color(0xFF121212) // Standard dark theme background
val DarkSurface = Color(0xFF1E1E1E)      // Elevated surfaces in dark mode
val DarkOnPrimary = Color(0xFFFFFFFF)    // Text/icons on primary color
val DarkOnBackground = Color(0xFFE0E0E0)  // Light grey text for readability
val DarkOnSurface = Color(0xFFCCCCCC)     // Secondary text in dark mode
val DarkOutline = Color(0xFF424242)      // Dividers and outlines in dark mode