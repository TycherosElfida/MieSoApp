package com.mieso.app.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme Palette
val BrandRedOrange = Color(0xFFE65100)  // A strong, warm orange-red for primary actions (FABs, buttons)
val BrandOrangeLight = Color(0xFFFF8A65) // A lighter accent for secondary elements
val BrandYellow = Color(0xFFFFCA28)   // A warm yellow for highlights, stars, or promo tags

val LightBackground = Color(0xFFFFF7F4) // A very light, warm off-white for main background
val LightSurface = Color(0xFFFFFFFF)     // Pure white for cards, sheets, and elevated surfaces
val LightOnPrimary = Color(0xFFFFFFFF)   // Text/icons on top of the primary color (e.g., on a button)
val LightOnBackground = Color(0xFF212121) // Dark text for high readability on light backgrounds
val LightOnSurface = Color(0xFF424242)    // Slightly lighter text for secondary information
val LightOutline = Color(0xFFE0E0E0)     // For borders, dividers, etc.

// Dark Theme Palette
val BrandRedOrangeDark = Color(0xFFFF6E40) // A slightly brighter orange for better visibility on dark backgrounds
val BrandOrangeDark = Color(0xFFFF9E80)  // Lighter accent for the dark theme

val DarkBackground = Color(0xFF1C1B1F) // A deep, near-black for the main background
val DarkSurface = Color(0xFF2B292E)      // A slightly lighter grey for cards and elevated surfaces
val DarkOnPrimary = Color(0xFFFFFFFF)    // Text/icons on top of the primary color
val DarkOnBackground = Color(0xFFE6E1E5)  // Light grey text for high readability on dark backgrounds
val DarkOnSurface = Color(0xFFD0C9CF)     // Slightly darker light grey for secondary info
val DarkOutline = Color(0xFF49454F)      // For borders and dividers in dark mode